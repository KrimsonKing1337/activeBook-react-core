import { Theme } from '../../@types.js';
export interface State {
    theme: Theme;
    welcomeTour: boolean;
    vibration: boolean;
    flashlight: boolean;
    authorComments: boolean;
    fontSize: number;
    lineHeight: number;
}
