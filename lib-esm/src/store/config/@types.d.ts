import { Theme } from '@types';
export interface State {
    theme: Theme;
    welcomeTour: boolean;
    vibration: boolean;
    flashlight: boolean;
    authorComments: boolean;
    fontSize: number;
    lineHeight: number;
}
